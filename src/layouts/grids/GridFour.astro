---
import type { GridProps } from "@/types";

type Props = GridProps;

const { class: className } = Astro.props;
---

<section class:list={["col-span-4", className]}>
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
    <slot />
  </div>
</section>
